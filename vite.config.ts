import { defineConfig } from 'vite';

export default defineConfig({
  root: './',  // The root of your project, typically the project root
  build: {
    outDir: 'build',  // Output directory for build assets
  },
});
